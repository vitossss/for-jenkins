#!groovy 
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Crate a docker image') {
            steps {
                echo "Build was started"
                dir ('docker') {
                    sh 'docker build . '
                }
            }
        }
    }
}
